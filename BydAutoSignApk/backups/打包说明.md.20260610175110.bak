# 比亚迪自动签到 APK 打包说明

## 项目位置

`E:\AI脚本\BydAutoSignApk`

## 打包方式

1. 安装 Android Studio。
2. 用 Android Studio 打开 `E:\AI脚本\BydAutoSignApk`。
3. 等待 Gradle 同步完成。
4. 点击 `Build` -> `Build Bundle(s) / APK(s)` -> `Build APK(s)`。
5. APK 通常会生成在：

   `E:\AI脚本\BydAutoSignApk\app\build\outputs\apk\debug\app-debug.apk`

也可以在安装好 Android Studio/JDK 后运行：

```powershell
E:\AI脚本\BydAutoSignApk\build-apk.ps1
```

## 手机上使用

1. 安装 APK。
2. 打开“比亚迪自动签到”。
3. 点击“开启无障碍服务”，在系统设置里开启“比亚迪自动签到服务”。
4. 返回 APK，点击“立即签到”可以马上执行一次。
5. 选择时间后点击“保存每日自动签到时间”，APK 会每天按该时间触发签到。
6. 不需要定时签到时，点击“取消每日自动签到”。
7. 手机重启或 APK 更新后，已保存的每日签到时间会自动恢复。

## 注意

- 安卓系统不允许普通 APK 静默控制其他 APP，所以必须手动开启无障碍服务。
- 部分国产安卓系统需要允许本 APK 后台运行、自启动、忽略电池优化，否则定时触发可能被系统拦截。
- 如果手机重启后没有按时触发，请在系统管家/电池设置里允许“比亚迪自动签到”自启动。
- 首次运行建议观察一次，确认它能正确进入“我的”并点击右上角“签到”。
- 如果比亚迪 APP 页面文案或布局变化，需要调整源码里的匹配文案或兜底坐标。
